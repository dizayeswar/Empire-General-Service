# Local-only ASaaS documentation (not published to GitHub)
docs/ASAAS-*

# Node dependencies (installed locally only)
node_modules/
scripts/node_modules/

# Supabase migration exports (may contain passwords — never commit)
migration-data/
!migration-data/.gitkeep

# Supabase CLI local cache
supabase/.temp/

# Local Word extract used only to match HR-F-06 print layout
_hr_f06_docx/
